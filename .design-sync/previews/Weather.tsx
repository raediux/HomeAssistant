import { Weather } from 'home-assistant-react';

export function Default() {
  return (
    <div style={{ background: '#111110', padding: '12px 16px', display: 'inline-flex' }}>
      <Weather />
    </div>
  );
}
